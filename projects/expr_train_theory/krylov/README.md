# Krylov checks

NumPy implementation of Lanczos and Arnoldi with a few small diagnostics and tests. This code is used to compare possible Krylov constructions before connecting one of them to the shared QNN.

```bash
python -m projects.expr_train_theory.krylov.run_validation
```

# Circuit orbit check

Compares the span of intermediate layer states with the Arnoldi space generated by repeatedly applying the complete circuit.

```bash
python -m projects.expr_train_theory.krylov.circuit_orbits.run_study
```

# Krylov benchmark

Runs the basic Krylov diagnostics for a few small Hamiltonian/state pairs and saves time-resolved values plus a summary.

```bash
python -m projects.expr_train_theory.krylov.experiments.run_benchmark
```

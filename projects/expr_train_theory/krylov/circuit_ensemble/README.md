# Circuit ensemble

Repeats the circuit-orbit check over random initialisations and several entangling topologies. It also records average one-qubit entanglement entropy.

```bash
python -m projects.expr_train_theory.krylov.circuit_ensemble.run_study
```

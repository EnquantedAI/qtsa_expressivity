# Metric screening

Reports variation, simple depth/topology effect sizes and the strongest rank correlation for each diagnostic.

```bash
python -m projects.expr_train_theory.krylov.metric_screening.run_study
```

# QFIM

Reference implementation of the pure-state QFIM using central finite differences. The analytical examples are in `validation_models.py`; the PennyLane adapter is in `pennylane_adapter.py`.

```bash
python -m projects.expr_train_theory.qfim.run_validation
```
